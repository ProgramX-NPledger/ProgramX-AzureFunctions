{
  // Committed file — placeholders only, no credentials.
  // Put real values in http-client.private.env.json under a matching environment
  // name ("Local"); JetBrains merges that file over this one and it is gitignored.
  "Local": {
    "host": "https://localhost:7276",
    "token": ""
  }
}
